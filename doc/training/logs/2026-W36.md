# Learning Log - 2026-W36

## 2026-08-31 - Issue #6 backend foundation design

### Decisions and reasoning

- Chose `uv` for the independently buildable Python project under `apps/api/`, with
  `pyproject.toml`, a committed lockfile, and separate runtime and development dependencies.
- Chose to let the API start during a temporary PostgreSQL outage. Liveness remains independent of
  the database, while database-aware readiness returns a safe `503` until PostgreSQL recovers.
- Defined a minimal health response contract that reports database availability without returning
  exception text or connection details.
- Chose typed settings for environment, logging, the database URL, and the readiness connection
  timeout. Local defaults use disposable Compose-only credentials; production must override the
  database URL.
- Distinguished invalid configuration, which fails startup, from temporary database unavailability,
  which leaves the process live but not ready.
- Chose FastAPI lifespan startup and shutdown for database engine construction and disposal.
- Selected the minimal runtime set: FastAPI, Pydantic, pydantic-settings, SQLAlchemy, Psycopg 3 with
  its binary extra, and Uvicorn with standard extras.
- Selected pytest, HTTPX, and Ruff as development dependencies. Deferred Alembic, async test
  plugins, coverage tooling, and Testcontainers because Issue #6 does not yet require them.
- Chose synchronous SQLAlchemy for the initial service. It keeps connection lifecycle and endpoint
  tests small while synchronous FastAPI route functions avoid blocking the event loop.
- Chose a minimal `apps/api/app` package with separate configuration, database, health, and
  application-factory modules. Deferred empty domain-oriented packages until later features require
  them.
- Separated fast unit tests from PostgreSQL integration tests so their dependency and execution
  contracts remain explicit.
- Kept `compose.yaml` at the repository root for shared local infrastructure and API-specific
  environment examples, package metadata, lockfile, and commands inside `apps/api/`.
- Chose an application factory and Uvicorn `--factory` target to avoid settings and database resource
  construction as module-import side effects.

### Verification status

- These are design decisions only. No FastAPI application, Compose service, automated test, or
  successful backend command exists yet.

### Next action

Scaffold the accepted `apps/api/` boundary and verify the first independently runnable liveness
endpoint before adding PostgreSQL readiness behavior.

## 2026-08-31 - `uv` scaffold and liveness implementation

### Implemented

- Added the independently managed `apps/api/` Python project, Python 3.12 pin, direct runtime and
  development dependencies, and committed lockfile.
- Added `create_app()` as the application composition boundary without a module-level FastAPI
  instance.
- Added an isolated `/health/live` router and a Pydantic response model constrained to the literal
  status `ok`.
- Added an HTTP-level unit test that constructs the application through the factory and verifies the
  exact status code and JSON response.
- Added `apps/api/README.md` as the canonical API command reference and kept the Issue #6 note
  focused on design rationale and verification history.

### Failure and correction

- The first `uv run pytest tests/unit -q` attempt failed during collection with
  `ModuleNotFoundError: No module named 'app'` because the pytest console entry point did not place
  the API root on the import path.
- Added `pythonpath = ["."]` to pytest configuration in `pyproject.toml`. The repeated test passed.
- The passing test emits one upstream FastAPI `TestClient`/`httpx` deprecation warning. It is
  recorded for dependency review rather than suppressed without understanding it.

### Verification

- `uv run pytest tests/unit -q`: one passed.
- `uv run ruff check .`: passed.
- `uv run ruff format --check .`: passed; four files already formatted.
- Uvicorn factory startup and graceful shutdown succeeded.
- A real liveness request returned `200` with exactly `{ "status": "ok" }`.

### Next action

Implement typed configuration and its unit tests, including production rejection of the disposable
local database URL and a recognizable fake-secret non-disclosure test, before adding database
lifecycle or readiness.

## 2026-09-01 - Typed and secret-safe configuration

### Implemented

- Added frozen Pydantic settings for application environment, log level, secret database URL, and a
  connection timeout bounded from 1 through 10 seconds.
- Added syntax and driver validation for `postgresql+psycopg` URLs without opening a connection.
- Added a safe `ConfigurationError` boundary that reports invalid environment-variable names without
  forwarding raw Pydantic input or exception chaining.
- Rejected the disposable local database URL when `APP_ENV=production`.
- Loaded settings during FastAPI lifespan and stored them in `app.state.settings`; module import and
  `create_app()` remain free of configuration reads and resource creation.
- Added `.env.example` and documented the configuration contract in the API README.

### Failure and correction

- The first Ruff run rejected nested context managers in the startup-failure test. Combined them
  into one `with` statement as recommended.
- The first format check found two layouts in `test_config.py` that differed from Ruff's canonical
  formatting. Ran Ruff format on that file and repeated the full verification suite.
- The editor flagged `AsyncIterator[None]` as deprecated for the yielding `@asynccontextmanager`
  lifespan function. Replaced it with the more precise Python 3.12-compatible
  `AsyncGenerator[None, None]` annotation.
- The existing upstream FastAPI `TestClient`/`httpx` deprecation warning remains visible and
  unchanged.

### Verification

- `uv run pytest tests/unit -q`: eleven passed, one upstream warning.
- `uv run ruff check .`: passed.
- `uv run ruff format --check .`: passed; seven files already formatted on the final run.
- Normal Uvicorn startup and graceful shutdown succeeded after settings were wired into lifespan.
- A real liveness request still returned `200` with exactly `{ "status": "ok" }`.
- Deliberately invalid production startup exited with code 3 and named only `DATABASE_URL`; its fake
  password and complete connection URL were absent from output.

### Next action

Implement SQLAlchemy engine construction and disposal in `app/database.py`, wire the resource into
FastAPI lifespan without connecting at startup, and verify lifecycle behavior with controlled unit
substitutes before adding Compose or readiness.

## 2026-09-01 - SQLAlchemy engine lifecycle

### Implemented

- Added `app/database.py` as the owner of SQLAlchemy engine construction and disposal.
- Passed the secret database URL only to SQLAlchemy, applied the configured Psycopg connection
  timeout, and enabled `pool_pre_ping` for later stale-connection detection.
- Created the lazy engine during FastAPI lifespan, stored it in application state, and guaranteed
  pool disposal through a `finally` block.
- Added controlled unit tests for engine options, disposal, startup ordering, state ownership, and
  shutdown ordering without contacting PostgreSQL.

### Verification

- `uv run pytest tests/unit -q`: fourteen passed, one existing upstream warning.
- `uv run ruff check .`: passed.
- `uv run ruff format --check .`: passed; nine files formatted.
- `nc -zvw 1 127.0.0.1 5432`: connection refused, confirming PostgreSQL was unavailable for the
  process-level check.
- Uvicorn still completed startup, a real liveness request returned `200` with exactly
  `{ "status": "ok" }`, and lifespan shutdown completed cleanly.

### Next action

Add root `compose.yaml` with disposable local PostgreSQL, implement the bounded `SELECT 1` readiness
probe and `/health/ready`, and verify both available and unavailable database states through a
PostgreSQL integration test.
