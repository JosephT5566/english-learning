# 2026-W39

## Issue #39 — isolated embedding lifecycle rehearsal

- The operator completed the migration upgrade/downgrade cycle and runtime privilege checks on an
  isolated Neon branch containing 597 cards and review states.
- A synthetic request using local service-account impersonation returned 512 finite, nonzero
  values without truncation. An owner-bounded private-card backfill then completed in resumable
  batches: aggregate checks showed 597 ready embeddings, 597 card hashes, unchanged card/review
  counts, no other states, and zero hash mismatches.
- A cursor-free replay returned no eligible work, supporting unchanged-content idempotency. These
  results verify the isolated operator path, not production deployment, Cloud Run workload identity,
  or real-card retrieval quality.

## Issue #39 — production embedding backfill

- The operator reported a successful production migration and zero-traffic candidate deployment.
  A dedicated Cloud Run Job initially failed safely because production-required non-secret settings
  were missing; correcting its configuration allowed a two-page 597-card dry run.
- The production backfill then completed in bounded batches through the API runtime identity.
  Aggregate SQL reported 597 ready embeddings and card hashes, unchanged card/review counts, zero
  hash mismatches, and zero missing current embeddings. A cursor-free replay returned no work.
- The replacement zero-traffic candidate included the Vertex project/location settings and passed
  the operator's environment smoke test. Traffic promotion and retrieval quality remain unverified.
