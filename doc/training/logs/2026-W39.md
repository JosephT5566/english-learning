# 2026-W39

## Issue #39 — isolated embedding lifecycle rehearsal

- The operator completed the migration upgrade/downgrade cycle and runtime privilege checks on an
  isolated Neon branch containing 597 cards and review states.
- A synthetic request using local service-account impersonation returned 512 finite, nonzero
  values without truncation. An owner-bounded private-card backfill then completed in resumable
  batches: aggregate checks showed 597 ready embeddings, 597 card hashes, unchanged card/review
  counts, no other states, and zero hash mismatches.
- A cursor-free replay returned no eligible work, supporting unchanged-content idempotency. These
  results verify the isolated operator path, not production deployment, Cloud Run workload identity,
  or real-card retrieval quality.

## Issue #39 — production embedding backfill

- The operator reported a successful production migration and zero-traffic candidate deployment.
  A dedicated Cloud Run Job initially failed safely because production-required non-secret settings
  were missing; correcting its configuration allowed a two-page 597-card dry run.
- The production backfill then completed in bounded batches through the API runtime identity.
  Aggregate SQL reported 597 ready embeddings and card hashes, unchanged card/review counts, zero
  hash mismatches, and zero missing current embeddings. A cursor-free replay returned no work.
- The replacement zero-traffic candidate included the Vertex project/location settings and passed
  the operator's environment smoke test. Traffic promotion and retrieval quality remain unverified.

## Issue #40 — owner-safe retrieval implementation

- Added the authenticated exact pgvector search boundary and a base-path-safe Svelte search page.
  Deterministic real-PostgreSQL tests passed for ranking, owner/archive/missing-vector exclusion,
  pre-provider rejection, and retryable provider failure. The full 289-test backend suite, generated
  contract, frontend/static build checks, and one critical browser flow passed; provider-backed
  candidate observations remain.

## Issue #40 — production exact-query plan inspection

- The operator supplied a read-only Neon plan and coverage check: 596/596 eligible English cards and
  1/1 Japanese card had current embeddings. After correcting probe CTE shapes, the final warm plan
  ranked 596 English candidates in 10.054 ms with small-table sequential scans, an in-memory hash
  join, a 27 kB top-N heapsort, zero shared reads, and no temporary I/O. Earlier 10-12 ms CTE-heavy
  plans and one 128.336 ms read-heavy observation were retained with their limitations. This is a
  single SQL Editor observation, not p50/p95 or Cloud Run end-to-end latency; no HNSW need was found.

## Issue #40 — semantic candidate smoke gate

- Extended the protected candidate smoke path with an explicit one-request semantic-search opt-in.
  The validator checks HTTP/request correlation, bounded results, coverage consistency, score and
  stable-distance ordering, and rejects raw-vector fields. Logs and the workflow summary retain only
  safe counts, request ID, index status, and one curl total-time observation; no card content or raw
  response is emitted. Deployment and the actual provider-backed observation remain pending.

## Issue #40 — zero-traffic candidate verification

- The operator reported that revision `english-learning-api-98034f911a63` passed the protected
  candidate smoke: authenticated owned read, controlled review write with exact replay, and the
  explicitly enabled provider-backed semantic request all succeeded. Semantic search returned five
  results with complete 596/596 coverage and request ID
  `f54491aa-b291-4d7a-b142-42035b4feb67`; curl observed 2.058828 seconds for this single end-to-end
  request. This is an operator-reported point observation, not a latency distribution or quality
  claim. Promotion and stable-URL verification remain.

## Issue #40 — promotion and stable-URL verification

- The operator promoted revision `english-learning-api-98034f911a63` and reported that the stable
  Cloud Run URL passed public health, authenticated owned-read, and the same bounded provider-backed
  semantic smoke. It returned HTTP 200 with five results, complete 596/596 coverage, request ID
  `d581e030-222e-497d-9c1b-38ea4bd7b9b3`, and a 2.265995-second curl observation. Review write was
  intentionally not repeated because it had passed on the candidate. This remains a point latency
  observation and contract smoke, not a retrieval-quality claim.

## Issue #40 — deployed search-page verification

- The operator reported that the deployed application's search page worked correctly against the
  promoted API. No query text, card content, card IDs, or credentials were retained. This verifies
  the deployed browser integration path; the bounded expected-result quality check remains.

## Issue #40 — bounded quality smoke and closeout

- The operator inspected the deployed fixed recovery-after-difficulty query and judged the Top-K
  check passed: an expected directly related concept appeared at rank 1, with additional contextual
  matches in the Top 5. Only the boolean result and rank bucket are retained, not response content or
  identifiers. Together with deterministic ownership/ranking tests, candidate and stable-URL smoke,
  and the deployed page check, this completes Issue #40. Broader quality, cost, and failure
  evaluation remains Issue #41.

## Issue #41 — retrieval evaluation and iterate decision

- Froze the quality gate before final results, rejected an initial deployed fixture after proving
  all of its targets were absent from the owned corpus, and reran a corpus-grounded v2 set whose
  eight sanitized targets each had one active card. Eight of nine positive queries had a strong
  Top-5 result and seven ranked first; ambiguous severance missed, while the negative control
  returned five irrelevant neighbors and exposed the lack of a no-answer threshold.
- The deployed 30-request workload returned 30/30 HTTP 200 with complete 596/596 coverage and
  end-to-end p50/p95 `1789.565/2793.855 ms`; one first request took `10824.294 ms`. The v2 cost probe
  reported 84 input tokens over 10 queries, provider p50/p95 `1280.6/1713.1 ms`, and published-rate
  estimated input cost `$0.0000126`; actual billing was not inspected.
- Eleven focused PostgreSQL/pgvector tests passed the empty/unindexed, partial, edit, archive,
  version, provider-outage, cross-owner, and ordering boundaries. The decision is iterate: keep the
  search/model/canonical text and exact scan, defer HNSW, and do not start tutor work until a frozen
  relevance/no-answer gate handles negative and ambiguous queries.
