# 2026-W37

## Issue #22 — read-only CSV import boundary

- Completed the local one-time CSV validator, revision `20260909_0004`, persisted import-run/item
  diagnostics, deterministic replay, all 21 field outcomes, and synthetic English/Japanese reports.
- Chose trimmed NFC for content/source IDs and NFKC plus case-folding for collection identity. Legacy
  scheduling is validated but reset so later imported cards begin fresh.
- Verified zero confirmed learning/review mutations, ownership/language conflicts, migration cycling,
  and safe diagnostics. The full backend suite passed 187 tests against PostgreSQL 17; Ruff,
  formatting, lock, and whitespace checks passed.
- The ignored private 596-row snapshot produced content-free aggregates of 11 accepted, 582
  repairable, and 3 rejected (one required meaning missing and one duplicate ID affecting two rows).
  No private values or report were committed.
- Next: resolve those three source rows, then design transactional apply and reconciliation before
  creating confirmed cards.

## Issue #23 — transactional confirmed import boundary

- Added revision `20260910_0005` with one-per-owner/source apply identity and composite,
  owner-constrained source-to-card mappings bound to the approved dry-run items and hashes.
- Implemented the local confirmed-import CLI: it rereads the private CSV, locks and reproduces the
  eligible dry run, atomically creates cards/tags/associations/fresh states/mappings, and returns
  mutation-free exact or concurrent replay.
- Verified seven pre-commit rollback points and recovery from a commit followed by client/report
  interruption. Post-commit reconciliation persists content-free counts, hashes, booleans, samples,
  and safe failure codes.
- Added sanitized reports, replay/failure evidence, and the freeze/cutover/rollback runbook. The
  full PostgreSQL-backed backend suite passed 220 tests with one existing upstream warning; Ruff,
  formatting, lock, migration-cycle, and whitespace checks passed.
- After correcting the three Issue #22 source diagnostics, the final zero-rejection private snapshot
  was applied locally. Reconciliation matched expected and actual counts: 596 cards, mappings, and
  fresh review states; 184 tags; 885 card/tag associations; zero archived cards; and no review
  batches or events. All checks passed with no diagnostic codes. The private CSV and operational
  reports remain untracked.
- No operator result for an unchanged replay or backup/restore drill was recorded. Google Sheets
  remains authoritative until a later explicit frontend cutover.

## Issue #24 — authenticated review API cutover

- Replaced the English review route's Apps Script reads/writes with typed, runtime-validated
  `GET /v1/reviews/due` and `POST /v1/reviews` calls. Google tokens travel only in the bearer header;
  the swipe component emits decisions and state versions while FastAPI owns scheduling.
- Persisted one exact account-scoped command/key pair for 24 hours. Retryable, ambiguous, and
  same-account auth recovery retain it; cross-account/invalid/expired storage, success, definite
  rejection, and optimistic conflicts retire it according to the documented matrix.
- Preserved flip-before-answer and separated answers-ready, submitting, unconfirmed, rejected,
  conflict, empty, authentication, and confirmed-success UI states. No review fallback or dual write
  exists; rollback is a previous-frontend redeploy with downtime and explicit PostgreSQL/Sheet
  divergence.
- Ten frontend contract/state/component tests, nine Playwright Chrome flows, static subpath build,
  targeted ESLint, Ruff/lock checks, and all 220 PostgreSQL-backed backend tests passed locally.
  Repository-wide Prettier lint remains at its known failing baseline; remote CI, live Google,
  deployment, and production behavior remain unverified.
- A real browser attempt exposed the missing cross-origin preflight boundary: bearer-authenticated
  requests received `405` on `OPTIONS`. Added validated exact-origin CORS configuration, restricted
  methods/headers, request-ID exposure, and five focused contracts. Real Uvicorn preflights now pass,
  and Chrome loaded 10 due cards with a live Google token from local FastAPI/PostgreSQL. Ruff,
  formatting, the lock check, and all 231 PostgreSQL-backed backend tests passed after the change.
- Fixed the real page's collapsed percentage-height chain and added a mobile browser regression,
  bringing the Playwright suite to 10 passing flows. The user then completed the live 10-card review;
  Chrome reported 10 saved answers, while a content-free PostgreSQL check confirmed one batch, 10
  events for 10 distinct cards, and 10 current states matching their recorded `srs-v1` results.
- Preserved the original imported type label in the review UI: canonical `other` cards now display
  `part_of_speech_detail` such as `vocabulary`, with a component regression bringing the frontend
  contract/state/component total to 11 passing tests.
