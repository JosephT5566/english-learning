# 2026-W38

## Issue #26 — protected production schema upgrades

- The operator reported an initial Cloud Run deployment, local Alembic upgrade against Neon, and a
  `pg_dump`/`pg_restore` data transfer. The deployed API returned
  `{"status":"ready","checks":{"database":"ok"}}`, confirming the reported runtime database probe;
  authenticated behavior and restored-data reconciliation remain unverified from repository
  evidence.
- Added a manual production migration workflow that uses GitHub OIDC instead of a service-account
  key, keeps the Neon URL in GCP Secret Manager, serializes runs, requires an existing immutable
  commit-tagged image and explicit confirmation, and invokes a one-task zero-retry Cloud Run job.
- A separate Cloud Run execution runs `alembic current --check-heads` after upgrade, followed by the
  deployed API's database-readiness check through its runtime role. Data restore, downgrade,
  application deployment, and traffic promotion remain deliberately separate.
- Artifact Registry `asia-east1` and Cloud Run `asia-southeast1` are now independent release
  settings. Remote workflow execution, live authorization checks, data reconciliation, and rollback
  rehearsal remain pending.

## Issue #26 — protected API image publishing

- Added a separate manual image-publishing workflow that authenticates through WIF, requires an
  explicit confirmation and protected environment, refuses to overwrite commit tags, builds the API
  for `linux/amd64`, pushes to Artifact Registry, and verifies the result.
- The publisher uses a dedicated Artifact-Registry-Writer identity and cannot manage Cloud Run,
  impersonate the migration job identity, or read database secrets. Migration and deployment remain
  separate operations; remote workflow execution is not yet verified.

## Issue #26 — live promotion and rollback rehearsal

- The operator reported a deployed GitHub Pages frontend with successful authenticated read/create
  API calls. The production smoke script then passed public health, authenticated owned reads, one
  controlled review write, and an exact same-key idempotent replay.
- Smoke checks subsequently passed against the zero-traffic candidate, after promotion, and after
  routing traffic back to the compatible previous revision. This is operator-reported evidence of
  the staged-release and application-rollback boundary; no automatic Alembic downgrade was used.
- Revision identifiers, timings, complete restored-data reconciliation, role-separation evidence,
  and remote image-publish/migration workflow runs remain unrecorded.

## Issue #26 — WIF release automation

- The operator reported that the protected image-publishing and production-migration workflows both
  ran successfully through WIF. This verifies the remote OIDC authentication, Artifact Registry
  publication, Cloud Run migration job, Alembic head check, and runtime readiness path at the
  operator-reported level; run IDs remain unrecorded.
- Added a third protected workflow that deploys the same commit-tagged image as a zero-traffic
  candidate, verifies the candidate tag/traffic state and public health, and records the previous
  traffic allocation for smoke and rollback. It deliberately cannot build, migrate, promote, or
  roll back; remote execution remains pending.

## Training context budget correction

- Compressed project memory from a milestone-by-milestone history into a bounded active-state index
  with explicit repository-verified, operator-reported, and remaining-gap sections.
- Moved reusable prompt recipes and the optional `ai-ready` path out of the startup README into an
  opt-in session workflow. Required startup documentation fell from 5,524 to 2,714 words while
  canonical issue artifacts, evidence, and historical logs remained intact.
